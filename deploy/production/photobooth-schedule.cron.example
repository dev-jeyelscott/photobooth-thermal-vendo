# Install with `sudo crontab -u __APP_USER__ -e`, replacing __APP_ROOT__.
# Laravel schedules session expiration every minute and media pruning hourly.
* * * * * cd __APP_ROOT__ && /usr/bin/php artisan schedule:run >> /dev/null 2>&1
